# Data entry points

The supported entry point is `python -m gsm_memory.data.cli`. It provides thin
commands for source materialization, inventory, candidate build/validation,
logical comparison, freeze, and strictly read-only frozen verification. Run
`uv run --extra dev python -m gsm_memory.data.cli --help` for the exact options.
