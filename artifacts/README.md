# Rebuildable artifacts

Indexes, embeddings, and materialized graph snapshots belong here. They are
outputs of a pinned dataset/configuration and are not canonical source data.
Record build manifests and hashes so each artifact can be reproduced.
