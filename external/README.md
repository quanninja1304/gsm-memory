# External sources

`policy_green_sm_corpus/` is an upstream checkout containing the collected
Green SM policy corpus. Its nested `.git` directory is intentionally preserved.
Treat it as read-only source material; derived, reviewed, and frozen artifacts
belong under `data/releases/<dataset_version>/`.
