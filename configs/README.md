# Configuration

- `datasets/` — generator seeds, release versions, and scale tiers.
- `benchmark/` — pinned retrieval and graph-baseline settings.

Configuration files should be immutable per recorded run and referenced by hash.
