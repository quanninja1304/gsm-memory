# Dataset configurations

Generator seeds, schema/release versions, scale tiers, and publication clocks.

`gsm-dev-core-0.2.1.json` is the current configuration. The frozen `0.2`
release remains immutable but is deprecated and must not be used for new
experiments.
