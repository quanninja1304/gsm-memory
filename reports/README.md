# Reports

Human-facing analyses derived from immutable run outputs. Keep generated tables
and figures linked to run IDs and dataset/config hashes; do not hand-edit values
that should be regenerated from `runs/`.
